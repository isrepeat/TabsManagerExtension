# Git хранит текст и выдаёт его в рабочую копию с окончаниями строк LF.
# Это предотвращает смешивание LF и CRLF при редактировании внешними инструментами.
* text=auto eol=lf

# Эти файлы бинарные: Git не должен пытаться преобразовывать в них окончания строк.
*.png binary
*.exe binary
