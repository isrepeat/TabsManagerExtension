<UserControl x:Class="TabsManagerExtension.Controls.VirtualMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:local="clr-namespace:TabsManagerExtension"
             xmlns:localHelpers="clr-namespace:Helpers"
             xmlns:localControls="clr-namespace:TabsManagerExtension.Controls"
             x:Name="RootControl"
             Width="200"
             >
    <localControls:MenuControl 
        x:Name="VirtualMenu"
        MenuItems="{Binding VirtualMenuItems, ElementName=RootControl}"
        OpenCommand="{Binding OnVirtualMenuOpenCommand, ElementName=RootControl}"
        CloseCommand="{Binding OnVirtualMenuClosedCommand, ElementName=RootControl}"
        />
</UserControl>
